version: 0.1

phases:
  install:
    commands:
      - pip install -t $TARGET_DIRECTORY -r requirements.txt
      - mkdir target
      - cp -r $TARGET_DIRECTORY/* target/
  pre_build:
    commands:
      - echo hoge
  build:
    commands:
      - echo hoge
artifacts:
  type: zip
  files:
    - "target/*"
  discard-paths: no
